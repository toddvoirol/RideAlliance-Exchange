package com.clearinghouse.mcp;

import org.springframework.context.annotation.Configuration;

// import org.springframework.ai.mcp.server.McpServerFeatures; // Uncomment and adjust if available

@Configuration
public class McpInquireToolConfig {
    // This config is no longer needed with the annotation-based approach, but can be kept for future MCP tool beans.
}
